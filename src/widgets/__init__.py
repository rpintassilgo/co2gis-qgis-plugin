"""Small, reusable Qt widget builders shared across the dialog tabs.

These helpers consolidate UI patterns that were copy-pasted across the
``setup_*_tab`` functions (the output-path + Browse row, the styled GroupBox)
so the tab code stays declarative and the look stays consistent.
"""
