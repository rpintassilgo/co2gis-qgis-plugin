"""Pure-data constants for the plugin, organised by topic.

These modules hold the single source of truth for COMET / engineering default
values. They contain data only — no Qt, no processing, no logic — so they can be
imported from both the domain code (``src.core``) and the UI (``src.ui``).
"""
