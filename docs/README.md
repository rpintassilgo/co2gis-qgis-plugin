# docs/

Image assets referenced by the main [`README.md`](../README.md).

Add the following two files here (e.g. the figures from the paper):

- **`plugin-interface.png`** — screenshot of the LeastCostPipeline plugin interface in QGIS (the tabbed workflow + log panel). *Paper Fig. 1.*
- **`example-route.png`** — an example optimal route on the map (e.g. the Sagres–Bragança scenario). *Paper Fig. 2.*

Until these PNGs are added, the image links in the README will appear broken on GitHub.
